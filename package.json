{
  "name": "clickcart-nano-commerce",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api:test": "npm --prefix api test",
    "web:build": "npm --prefix web run build",
    "gateway:pay": "npm --prefix api run gateway:pay"
  }
}
